  baseURL = "https://seokho-ham.github.io/"

  # [en, zh-cn, fr, ...] determines default content language

  defaultContentLanguage = "en"

  # language code

  languageCode = "en"
  title = "포키의 개발 기록"

  # Change the default theme to be use when building the site with Hugo

  theme = "LoveIt"
  summaryLength = 500

  [params]

  # LoveIt theme version

  version = "0.2.X"
  defaultTheme = "auto"

  [params.header]
    # desktop header mode ("fixed", "normal", "auto")
    desktopMode = "auto"
    # mobile header mode ("fixed", "normal", "auto")
    mobileMode = "auto"
  [params.footer]
    enable = true
    since = 2020
  [params.home.profile]
      enable = true
      avatarURL = "/images/profile.jpg"
      subtitle = "최고 수준의 퍼포먼스는 경험을 오래한다고 자동으로 얻어지는 것이 아니다."
      # whether to use typeit animation for subtitle
      typeit = true
      # whether to show social links
      social = true
  [params.social]
    GitHub = "seokho-ham"
    Email = "kalejuice02@gmail.com"
  [params.logo]
    url = "/images/profile.jpg"
    width = 127
    height = 40
  # Website Image Info just for SEO
  [params.image]
    url = "/images/profile.jpg"
    width = 800
    height = 600
  [params.page]
    lightgallery = true
    [params.page.share]
      enable = true
      Twitter = true
      Facebook = true
      Linkedin = true
      Blogger = true
    [params.page.code]
      # whether to show the copy button of the code block
      copy = true
      # the maximum number of lines of displayed code by default
      maxShownLines = 500
    [params.page.comment]
      enable = true
      [params.page.comment.utterances]
        enable = true
        repo="seokho-ham/blog-comments"
        issueTerm="pathname"
        label="Comment"
        lightTheme = "github-light"
        darkTheme = "github-dark"
    [params.analytics]
      enable = true
      # Google Analytics
      [params.analytics.google]
        id = "UA-213511127-1"
        anonymizeIP = true

[sitemap]
  changefreq = "weekly"
  filename = "sitemap.xml"
  priority = 0.5

[author]
  name = "포키"
  link = "https://seokho-ham.github.io"


  [menu]
  [[menu.main]]
  identifier = "posts" # you can add extra information before the name (HTML format is supported), such as icons
  pre = "" # you can add extra information after the name (HTML format is supported), such as icons
  post = ""
  name = "Posts"
  url = "/posts/" # title will be shown when you hover on this menu link
  title = ""
  weight = 1
  [[menu.main]]
  identifier = "tags"
  pre = ""
  post = ""
  name = "Tags"
  url = "/tags/"
  title = ""
  weight = 2
  [[menu.main]]
  identifier = "categories"
  pre = ""
  post = ""
  name = "Categories"
  url = "/categories/"
  title = ""
  weight = 3

  # Markup related configuration in Hugo

  [markup]

  # Syntax Highlighting (https://gohugo.io/content-management/syntax-highlighting)


  [markup.highlight]
    codeFences = true
    guessSyntax = true
    lineNoStart = 1
    lineNos = true
    lineNumbersInTable = true
    noClasses = false
    style = "monokai"
    tabWidth = 4
  [markup.goldmark]
    [markup.goldmark.renderer]
        unsafe= true